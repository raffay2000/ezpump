

const initialState = {
    pumps: [],
    loading: false,
}

export default (state = initialState, action) => {
    const {type} = action;
    switch (type) {
        case value:
            
            break;
    
        default:
            break;
    }
}