

const initialState = {

}

export default (state=initialState, action) => {
    const {type} = action;
    switch (type) {
        // case value:
            
        //     break;
    
        default:
            return state;
    }
}